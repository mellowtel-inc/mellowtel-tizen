# Tizen Web project descriptor (used by the VS Code Tizen extension).
profile: tv
type: web
buildResultPath: .buildResult
packaging:
  type: wgt
sign:
  # Replace with your Samsung certificate profile name (Certificate Manager).
  profile: default
deploy:
  # Target id from `sdb devices` (TV emulator or a connected device).
  target: ""
